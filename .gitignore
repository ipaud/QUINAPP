node_modules
.DS_Store
*.log

# Build artifacts (packaged app, ~370 MB)
dist/

# macOS shortcut app (generat amb npm run app:macos; específic de màquina)
QUINAPP.app/

# Runtime pid/log files
.quinapp-*.pid

# Local SQLite database + per-install QR secret + self-signed TLS cert
data/*.db
data/*.db-*
data/qr-secret.key
data/*.pem
data/cert.meta.json

# Env
.env

# Android build outputs
apps/android/build/
apps/android/app/build/
apps/android/.gradle/
apps/android/local.properties
*.apk
